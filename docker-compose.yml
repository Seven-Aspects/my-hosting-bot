services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: my-hosting-bot
    restart: unless-stopped
    working_dir: /app
    command: ["python", "main.py", "supervisor"]
    volumes:
      - ./:/app
